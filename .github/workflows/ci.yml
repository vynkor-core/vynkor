name: CI

on:
  push:
    branches: [develop, main]
  pull_request:
    branches: [develop, main]

env:
  CARGO_TERM_COLOR: always

jobs:
  test:
    name: Test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      # SDKs/wire live in sibling repos (not submodules — temporary decision,
      # see ROADMAP). Clone them beside the workspace so tests and the
      # proto drift check can reach their sources via ../veyron-* paths.
      # Plain git clone: actions/checkout@v4 refuses `path: ../` (repo path
      # must stay under the workspace) since runner 2.319+.
      - name: Clone veyron-wire (sibling)
        run: git clone --depth 1 https://github.com/veyron-core/veyron-wire.git ../veyron-wire
      - name: Clone veyron-sdk-cpp (sibling)
        run: git clone --depth 1 https://github.com/veyron-core/veyron-sdk-cpp.git ../veyron-sdk-cpp
      - name: Clone veyron-sdk-python (sibling)
        run: git clone --depth 1 https://github.com/veyron-core/veyron-sdk-python.git ../veyron-sdk-python

      - name: Proto drift check (T-17)
        run: |
          diff -u ../veyron-wire/proto/veyron_protocol.proto ../veyron-sdk-cpp/proto/veyron_protocol.proto
          diff -u ../veyron-wire/proto/veyron_protocol.proto ../veyron-sdk-python/proto/veyron_protocol.proto

      - name: Install stable toolchain
        uses: dtolnay/rust-toolchain@stable
        with:
          components: clippy, rustfmt

      - name: Cache cargo
        uses: Swatinem/rust-cache@v2

      - name: Install C++ SDK build deps
        run: |
          sudo apt-get update
          sudo apt-get install -y --no-install-recommends \
            protobuf-compiler libprotobuf-dev libabsl-dev libssl-dev libzstd-dev libgtest-dev

      - name: "Build C++ SDK echo_plugin (T-02: real C++ binary for cpp integration test)"
        run: |
          cmake -B ../veyron-sdk-cpp/build -S ../veyron-sdk-cpp -DCMAKE_BUILD_TYPE=Release
          cmake --build ../veyron-sdk-cpp/build --target echo_plugin -j"$(nproc)"

      - name: Build
        run: cargo build --all --all-features

      - name: Unit + integration tests
        run: cargo test --all --all-features

      - name: Clippy
        run: cargo clippy --all -- -D warnings

      - name: Format check
        run: cargo fmt --check
